# Public release readiness — Bitcoin/Lightning, HBAR in Advanced options

Updated 22 September 2026. Status: **internal Android candidate; public release blocked by outstanding acceptance and independent review**. The agreed first-release experience leads with Bitcoin/Lightning; existing HBAR functionality is accessible through collapsed Advanced options. Swap execution, additional assets and identity payments remain outside this release. This document records implementation separately from completed device evidence.

Latest automated acceptance work: **413 app tests, TypeScript and changed-file lint passed**. Earlier evidence includes 9 local contract tests, expanded lint with zero errors and one existing array-style warning, zero known npm audit vulnerabilities and compatible Expo dependencies. Wallet-lifecycle and translation checks remain covered; twenty-two cases exercise request-index/operator recovery, pre-submit persistence boundaries and pending-state presentation; ten more cover scanner-first entry, automatic routing, camera lifecycle and safe clipboard handling. Four measurements of the previous APK reached fresh Bitcoin balance in **3.631 / 4.477 / 5.019 / 3.538 seconds**, with the cached balance rendered in 1.307–1.435 seconds. See [automated acceptance](TEST_ACCEPTANCE_2026-09-21.md) and the P05 follow-up below.

Owner-operated Android acceptance on 22 September now confirms device-PIN cancellation/unlock, payment authorization cancellation and successful submission, active-use/idle/background locking, scanner navigation, rejection of three invalid invoices, 100-SAT receipt, and a 30-SAT receipt while the app process was stopped followed by correct balance and single history entry after restart. A 20-SAT invoice payment delivered exactly 20 SAT for a 22-SAT balance deduction; a separate 20-SAT Lightning-address payment and LNURL-QR review also passed. An unpaid request hides its QR and shows expiry. P01 (regular sending), P02 (request/receive, including receipt after process stop and expiry display) and P03 are green in the feature matrix. Replacement after expiry and further receive failure handling remain open under full device acceptance R02; their results are not claimed. Outgoing interrupted-payment reconciliation (P05), clean second-device recovery, biometrics, iOS/accessibility and independent review remain outstanding. These results are owner reports, not agent-executed payments; see [22 September device acceptance](TEST_ACCEPTANCE_2026-09-22.md).

## Overlapping independent Lightning preparation

**Latest owner timing:** the installed pipeline candidate took **10.827 s after approval**, including 6.467 s for an internal leaf swap absent from the preceding run. Transfer/preimage preparation overlapped as intended and totalled 1.879 s; the final SSP call took 1.365 s. The five-second target remains unmet. Timing alone does not establish payment success. No further app changes after this measurement; [full analysis](LIGHTNING_SEND_PERFORMANCE.md).

Installed on Android …8690 at 23:35:50 MESZ on 22 September. SHA-256 `a59174b8d9e4bf58d247ed852fc3bca75d809629a33884fddd8e64938d1f9b3d` matches the verified APK; release/native tests, v2 signature, 91 source modules and first-install preservation checked. Locked launch had no observed startup errors. Owner-operated payment/timing acceptance for this update is pending. Evidence `.codex-local-evidence/lightning-pipeline-device-update.json`.

The native HTLC candidate measured **5.692 s after approval** (SDK 5.429 s), versus 9.190 s previously. Transfer preparation fell to 1.598 s and the refreshed balance finished only 41 ms after approval. The owner's five-second requirement remains unmet. Evidence: `.codex-local-evidence/lightning-htlc-native-capture.json`.

The next candidate overlaps fresh commitment retrieval/local normal-refund signing with HTLC package preparation. Original SDK wire requests, fee data, idempotency and outbound ordering are preserved; data is bound to one transfer/package and consumed once, with cancellation and cleanup guards. 413 app tests, TypeScript and changed-file lint passed, including original-SDK request comparisons and failure/concurrency tests. No agent-operated payment and no measured five-second outcome for this candidate yet. [Details](LIGHTNING_SEND_PERFORMANCE.md).

## Native HTLC preparation and approval-time balance freshness

404 app tests, TypeScript and changed-file lint passed. A narrow pinned-SDK Android adapter reuses the public HTLC output and delegates its scalar multiplication to existing native Spark/Rust. Byte-for-byte original-SDK transaction/sighash comparisons pass. Bounded balance refreshes during approval preserve the original five-second freshness rule, all cancellation/session gates and durable submission ordering. No agent-operated payment. The owner comparison of this second optimization is recorded above at 5.692 seconds after approval; the maximum-five-second requirement is still unmet. [Implementation and limits](LIGHTNING_SEND_PERFORMANCE.md).

Installed on Android …8690 at 23:16:32 MESZ. SHA-256 `0749b4c9aa6d6ce703d2b2e4466c63bdbbdab2dc625523555c2fc95c3280a70a` matches the verified artifact; APK-v2, 90 source modules, native packaging and first-install preservation checked. Locked launch succeeded without observed startup errors. No authenticated payment by the agent and no measured five-second result yet. Evidence `.codex-local-evidence/lightning-htlc-native-device-update.json`.

## Spark send follow-up: leaf selection and scoped key reuse

**Owner-run comparison obtained:** 9.190 s after authorization versus 25.400 s before (63.8% shorter in this sample); SDK 7.888 s versus 25.161 s. Total including device authorization is 16.315 s. No leaf swap was observed this time. Remaining transfer preparation takes 4.050 s; the longer approval correctly triggered a new balance check (1.125 s). The five-second target is still unmet. One before/after sample does not establish a repeatable code-only improvement; wallet structure/route/amount were deliberately not recorded. No functional acceptance is inferred from timing alone. Evidence: `.codex-local-evidence/lightning-leaf-selection-capture.json`; [full comparison](LIGHTNING_SEND_PERFORMANCE.md).

Latest automated checks: **390 app tests, TypeScript and changed-file lint passed**. Based on the owner-approved trace below, the next Android candidate adds bounded exact leaf selection, fewer inputs for required swaps, and per-send deterministic key reuse. Tests exercise the pinned SDK's actual reservation/swap/failure state machine with synthetic data. Nonces remain fresh and private cache copies are cleared on completion/error/cleanup. The single-owner comparison above is not broad device acceptance; no five-second claim, no P05 upgrade and no agent-operated payment. Anonymous timing remains enabled only for the owner's next comparison. Implementation and limits: [Lightning send performance](LIGHTNING_SEND_PERFORMANCE.md).

Release/native crypto checks, APK-v2 signature, 89 bundled source modules and native packaging passed. Candidate SHA-256 `8aa197cfdef013ea8b5308ac1b2c9254384aaa5e280f0cb5500bce6614d7cc4f`. Installed on Android …8690 at 22:57:31 MESZ after scrcpy-ADB connectivity returned; installed hash matches, original first-install timestamp preserved, locked launch successful with zero observed startup errors. No agent-operated unlock/payment and no device send-performance acceptance for this candidate. Evidence: `.codex-local-evidence/lightning-leaf-selection-device-update.json`.

## Explicitly approved send timing diagnostic — 22 September 2026

**Owner-run capture obtained:** 29.185 s total including authorization, 25.400 s after authorization, 25.161 s in `payLightningInvoice`. The dominant block is leaf selection/swap (15.519 s), followed by transfer preparation (5.447 s), preimage swap (2.170 s) and the final SSP send request (1.568 s). Nested signing-preparation work is substantial; do not add overlapping spans. Balance read and journal/proof checks are not the dominant delay in this sample. Five-second acceptance failed; SDK defaults already enable leaf optimization, so no missing simple switch was identified. See [measured findings](LIGHTNING_SEND_PERFORMANCE.md). No further payment-code changes in this diagnostic step.

After the owner reported a 25-second send, they explicitly approved anonymous per-stage timing for their next manually executed payment. An internal build flag `EXPO_PUBLIC_SEND_TIMING=true` enables one attempt per process; normal builds remain inactive. Fixed operation labels and numeric relative timings only, no arguments/results/errors/identifiers/amounts. SDK observers preserve invocation order and original promises; output is deferred until completion and bounded to 512 spans. Overlapping spans are not additive and shared SDK instances may include concurrent background activity. The device log reader filters the prefix before returning data, then validates its schema again.

375 app tests, TypeScript and changed-file lint passed for that diagnostic build. No agent-operated payment. The capture result is recorded above; the five-second target remains open. This diagnostic build is not a public-release artifact; remove its opt-in flag for regular builds.

Diagnostic release/native crypto checks passed. APK-v2 signature, 87 source modules and native packaging verified. Installed on Android …8690 at 22:31:44 MESZ with matching SHA-256 `7fc16039f3fbe6e72c1a3c4e05b93a2c1fe832f21c70d3aa20d2918c8dbdb1ad`; first-install timestamp preserved, locked startup without observed errors. The owner test and timing capture are complete; baseline retained separately. Evidence: `.codex-local-evidence/lightning-send-timing-*`.

## Android native Spark signer adapter — 22 September 2026

Owner follow-up after installing this adapter: **25 seconds** for a send. No demonstrated latency improvement; do not treat this candidate as meeting the five-second target. Per-stage timing was requested for explicit owner approval because earlier instructions excluded agent measurements. No new timing instrumentation has been enabled pending that answer.

Owner comparison: Opago takes 23 seconds; Wallet of Satoshi takes under five seconds on the same Android POS/WLAN. The previous preflight changes have not established an improvement. Hardware alone cannot explain that result. Source/bundle inspection confirms React Native/release packaging, with some default-signer curve multiplications still in JavaScript. An Android-only adapter now calls the existing Rust public-key and batched nonce-commitment methods. SDK derivation/randomness, FROST, payment route, authorization, fee ceiling, journal and proof checks are preserved. No cached/reused signing nonces. Native errors fail closed; iOS/web/missing optional methods retain the original signer. Review this extension and the bridge byte contract when updating pinned SDK 0.7.12.

370 app tests, TypeScript and changed-file lint passed. Synthetic correctness fixtures cover unchanged keys/signatures, Kotlin signed bytes, nonce ownership/uniqueness, malformed native results and sanitized failures. These tests mock the Android bridge with the original arithmetic; actual device payment/speed acceptance belongs to the owner. No timers/profiling or real payments were added/performed. Five-second target and P05 remain open.

Android release/native crypto checks succeeded. APK-v2 signature, 85 bundled source modules, Spark native registration/public-key bridge and ARM64 Rust library verified. Installed on Android …8690 at 22:20:58 MESZ; SHA-256 `9e79eb47e7b898d21ddc4326a59d67d285ff28f019e85918a2b98cf0aef814c0` matches the installed APK, first-install timestamp unchanged. Locked launch succeeded with zero observed startup errors. Authenticated payment and speed acceptance remain untested. Evidence: `.codex-local-evidence/lightning-native-signer-*`.

## Bitcoin-only send preflight — 22 September 2026

363 app tests, TypeScript and changed-file lint passed. A SparkWallet subclass retains pinned SDK 0.7.12 fresh AVAILABLE-leaf validation/recovery and cache eviction for payment balance checks, omitting unrelated token sync/metadata. Review still uses fresh data. On Send, balance preparation overlaps device authorization; checks older than five seconds are refreshed after approval. Snapshot binding, approved fee ceiling, native authorization, session/navigation cancellation, durable duplicate guard and verified proof remain required. The full SDK balance API is unchanged. Review this extension against SDK internals when changing the pinned SDK version.

The owner observed no meaningful improvement from the previous scheduling build and reported 15–20 second sends. This change has not established the five-second goal. No agent-operated payment measurements or new instrumentation; device speed acceptance remains with the owner. P05 and public-release blockers are unchanged.

Android release build and native crypto tests passed. APK-v2 signature, 84 bundled source modules and native modules verified. Update installed on Android …8690 at 21:55:10 MESZ; SHA-256 `c1e9154df37203d1f645b8c75a4749ad9013ec74e0f7e04ec10c0ff82f94fef5` matches the installed APK, first-install timestamp unchanged. Locked startup succeeded with no observed startup errors. Existing startup/payment diagnostics remain disabled. Evidence: `.codex-local-evidence/lightning-bitcoin-preflight-*`.

## Lightning send scheduling — 22 September 2026

348 app tests, TypeScript and changed-file lint passed. Review balance/fee queries run in parallel; durable payment state still precedes submission, while ordered, rebuildable history indexing runs without delaying submission or verified success. Terminal state and request ID share one write, with ID-only recovery fallback. No additional performance instrumentation or agent-operated payment measurements. Real-device speed acceptance remains with the owner; P05 and public-release blockers are unchanged.

Android release build and native crypto tests passed; APK-v2 signature, 81 bundled source modules and native modules verified. APK SHA-256: `569ab9940a12d6624d0e77af8fc6d7619dea5a0a982d4be3ee4a79ebf6282efd`. Existing startup/payment diagnostics are disabled for this candidate. Update installed on Android …8690 at 21:40:24 MESZ using scrcpy-bundled ADB with shell -T -x after earlier connection failures. Installed APK hash matches; first-install timestamp is unchanged. Locked startup succeeded with no observed startup errors. No authenticated payment or speed test was performed. Evidence: `.codex-local-evidence/lightning-send-scheduling-*`.

## Direct fixed-invoice review — 22 September 2026

341 app tests, TypeScript and changed-file lint passed. Fixed-amount Lightning scans display review preparation instead of a temporary amount-entry screen. Unknown fees are never displayed as zero, Send stays disabled until preparation completes, and cancelling a lookup invalidates its late result. Amountless and explicit onchain preparation paths remain intact. Device visual acceptance remains with the owner; P05 and public-release blockers are unchanged.

Android release build and native crypto tests passed. APK-v2 signature, 81 source modules and native-module packaging verified. Update installation on Android …8690 reported Success; the installer subsequently verified unchanged first-install timestamp and matching installed SHA-256 **`63f3742d1207cd43655cced2a5d177ea0312176077abd1678cdfd75789555055`** before failing at the launch check. ADB then reported offline; reconnect briefly restored enumeration but the next shell command closed. **Locked startup is unverified for this build**, and no authenticated test was performed. Owner can open and test the installed app manually. Evidence: `.codex-local-evidence/direct-invoice-review-{tests.log,android-build.log,source-hashes.json,device-update.json}`.

## Full-screen payment flow — 22 September 2026

337 app tests, TypeScript and changed-file lint passed. Amount entry and payment review are full-screen, without scanner content behind them. Send opens a live authorization/sending presentation, and confirmed Lightning completion displays a green checkmark. Reduced motion is respected. Failures and unknown results never become success based on elapsed time; onchain broadcast remains unconfirmed until network confirmation. Final device acceptance remains with the owner. P05 and public release blockers are unchanged.

Android release build and native crypto tests passed. APK-v2 signature, 81 bundled source modules and native module packaging verified. Installed at **20:52:16 MESZ** on Android …8690, package `com.opago.wallet.productioncandidate`, SHA-256 **`3e9bdf3cc7316567935ca31cd8a556ee1c3a6970935a2671d93822ec55d9308a`** matches the installed artifact. First installation unchanged. Locked startup 2799 ms, zero observed startup errors; no authenticated payment or interactive camera test. Evidence: `.codex-local-evidence/fullscreen-payment-{tests.log,android-build.log,source-hashes.json,device-update.json}`.

## Custom amount keypad and cancellation — 22 September 2026

331 app tests, TypeScript and changed-file lint passed. Bitcoin amount entry now uses in-app digit/delete keys, locale-aware EUR decimals and a visible amount instead of the system keyboard. Amount and review sheets expose Cancel next to the primary action and block dismissal during submission. Manual address entry retains native keyboard handling with modal viewport correction. Device visual/accessibility acceptance remains with the owner; payment guards and P05 status are unchanged.

Android release build and native crypto tests passed. APK-v2 signature, 80 bundled source modules, native module packaging and installed APK hash verified. Update installed at **20:38:16 MESZ** on Android …8690, SHA-256 **`2a8b269511be96b1ae7e12280e40f3f6f616eebe44ddba50c542d1104eba0251`**. First installation unchanged; locked launch 2829 ms with zero observed startup errors. No authenticated payment or interactive camera test performed. Evidence: `.codex-local-evidence/keyboard-actions-{tests.log,android-build.log,source-hashes.json,device-update.json}`.

## Direct scan continuation and Buy label — 22 September 2026

Home now labels the coming-soon middle action Buy. Validated QR, clipboard and manual input advance automatically into amount entry or fixed-Lightning cost review without the redundant recognition button. The recognized-scanner background remains. Explicit final confirmation, device authorization, one-time input handoff and onchain signing-preparation approval are preserved.

40 affected regression tests, TypeScript, changed-file lint, native crypto unit tests and Android build passed. APK-v2 signature and 79 source modules verified. SHA-256 **`b3952e94325e28b5e1fbf7977d6d3c1b074a2e926c8561ff815c12633e0602d7`** matches the update installed at **20:18:35 MESZ** on Android …8690 after USB reconnection. First-install timestamp unchanged; locked startup succeeded in 2757 ms with no observed startup errors. Interactive device acceptance remains with the owner; no payment performed. Evidence: `.codex-local-evidence/direct-scan-{tests.log,android-build.log,source-hashes.json,device-update.json}`.

## Shared scanner-success background — 22 September 2026, 20:09

Recognition, amount and review now use one shared scanner-success scene with identical header, gradient, corner-frame geometry and checkmark. Remove the separate Bitcoin-logo backdrop. Inline scan handoff sets its source immediately to avoid a transient legacy address form. The camera lifecycle, payment preparation and authorization remain unchanged.

33 affected regression tests, TypeScript, changed-file lint, native crypto unit tests and Android build passed. APK-v2 signature and 79 source modules verified. Installed at **20:09:39 MESZ** on Android …8690: SHA-256 **`2ec04ca4d6d5594a9257b06d85371f63a8f725916bd7b269977108bbd4695692`**, installed hash matches and first-install timestamp unchanged. Locked startup succeeded without observed startup errors. Visual/keyboard device acceptance remains with the owner; no camera interaction or payment performed. Evidence: `.codex-local-evidence/scanner-background-{tests.log,android-build.log,source-hashes.json,device-update.json}`.

## Receive symbols and HBAR card — 22 September 2026, 17:05

EUR/SAT receive selectors include currency symbols. HBAR on Home now matches the Bitcoin card with a unit exchange rate and asset balance; cached/missing prices keep explicit states. The hidden-entry reveal/conceal switch is removed; existing hidden records, reconciliation and duplicate protection remain. No new market-data calls.

30 affected regression tests, TypeScript, changed-file lint, native crypto unit tests and Android release build passed. Last full-suite run remains the preceding 324-test acceptance. APK-v2 signature and 78 source modules verified. Installed at **17:05:53 MESZ** on Android …8690: SHA-256 **`9311cd691855da0ad6d5b16b0040a09a6750683dd4c59963504b51da05f8a3bd`**, installed hash matches, first-install timestamp unchanged. Locked start succeeded with no observed startup errors. Visual device acceptance remains with the owner. Evidence: `.codex-local-evidence/asset-card-polish-{android-build.log,source-hashes.json,device-update.json}`.

## Compact Bitcoin payment sheets — 22 September 2026, 16:52

Amount entry and Bitcoin/Lightning review now use native bottom sheets. The amount step retains only the recipient, amount, SAT/EUR selection and Continue; review shows exact SAT amounts, fee cap, maximum total and Confirm and send. Full invoices, labels and technical fee information expand from the recipient row. Onchain preparation remains a distinct authorized action; no payment executes from recognition or amount entry. Sheets respect background/focus, keyboard and safe areas, and block dismissal during submission. The owner confirmed the earlier scanner correction works well; its camera lifecycle is unchanged here.

**324 app tests**, TypeScript, changed-file lint, native crypto unit tests and Android build passed. APK-v2 signature and 78 source modules verified; synthetic preview excluded. Installed at **16:52:07 MESZ** on Android …8690, SHA-256 **`dbdf0cc38ed6b8bc167cfc6e64bcb042ee00d31cdcc4aed5af6c91eef81adbc4`**, installed hash matches and original first-install time preserved. Locked launch succeeded in 2895 ms without observed startup errors. Native visual/keyboard/payment authorization acceptance is left to the owner; no payment or live-camera interaction performed. Evidence: `.codex-local-evidence/payment-sheets-{tests.log,android-build.log,source-hashes.json,device-update.json}`. Existing integration blockers and matrix status unchanged.

## Scanner lifecycle correction — 22 September 2026, 16:35

Restore continuous QR detection on a mounted camera and release it on capture, manual entry or backgrounding; retries create a fresh camera. Remove imperative pause/resume and handler enable/disable transitions, retaining the new full-screen design and all payment/session checks. The owner reported unreliable physical scanning after the redesign; the lifecycle race remains a suspected cause pending owner retest.

320 automated tests, TypeScript, changed-file lint, native crypto unit tests and Android release build passed. APK-v2 signature and 76 source modules verified; preview excluded. Update installed at **16:35:01 MESZ** on Android …8690. SHA-256 **`83ab8aa1be8738c38760de99c1885c6a4f0d6a121b7925ecd67e178774e4c1d1`** matches the installed APK; original first-install timestamp preserved. Locked startup succeeded in 2850 ms with zero observed startup errors. No live-camera interaction or payment performed. Evidence: `.codex-local-evidence/scanner-lifecycle-{tests.log,android-build.log,source-hashes.json,device-update.json}`. Physical QR acceptance remains with the owner; matrix status unchanged.

## Full-screen scanner — 22 September 2026, 16:17

The supplied concept is implemented with native full-screen camera, gradient overlays, open corners, capability-gated torch, a shared Paste/Type dock and keyboard-aware bottom sheets. A validated code produces a green recognized state and a separate Review payment action. Existing amount, fee, authorization and duplicate-payment checks remain in the payment pipeline. The exact shared tab-bar styling is restored when leaving the scanner.

Final validation: **319 app tests, TypeScript, changed-file lint, native crypto unit tests and Android release build passed**. Installed at **16:17:18 MESZ** on Android ending **8690**, package `com.opago.wallet.productioncandidate`. Installed SHA-256 **`3f52563eb39380005daf2eeaf56fed16a6e6983189fe50bd977a30f306b45e37`** matches the verified APK; v2 signature, 76 bundled source modules, native seed module and camera-capability module verified. Synthetic QA entry/data excluded. First install remains 16 September 2026, 15:42:19. Installation launch succeeded (locked cold launch 2928 ms), zero observed startup errors; no authenticated payment performed.

At the owner's request, final camera/interaction acceptance is handed back to the owner. Preliminary native QA screenshots preceded the final keyboard-focus and behind-sheet presentation fixes and are not final-build acceptance. Bright/dark QR scanning, physical torch behavior, final keyboard visibility, clipboard/back navigation, native accessibility and iOS remain unverified. The separate QA app was stopped. P05 remains unresolved; feature-matrix counts do not change. Evidence: `.codex-local-evidence/fullscreen-scanner-device-update.json`, `fullscreen-scanner-source-hashes.json`, `fullscreen-scanner-android-build.log`, and `fullscreen-scanner/{tests,typecheck,lint,final-targeted}.log`. See TEST_ACCEPTANCE_2026-09-22.md for the testing boundary.

## Scanner presentation update — 22 September 2026, 15:42

Full-width paste/manual actions, shorter camera instructions, corner markers and the manual address field replace the earlier presentation. 311 app tests, TypeScript, changed-file lint, native crypto unit tests and Android release build passed. Synthetic scanner layouts checked at 360×740 and 320×568; manual entry and return navigation checked at 360×740. Native camera and large-system-font acceptance are not claimed.

Installed at **15:42:48 MESZ** on Android ending **8690**, package `com.opago.wallet.productioncandidate`. Installed SHA-256 **`f1e0214a46815bef8c7f236f9cf2a68c40e7840746ca7a87300201ca44966682`** matches the verified APK. V2 signature, 70 source modules and native seed module checked; synthetic preview excluded. First install remains 16 September 2026, 15:42:19. Locked cold launch **2879 ms**, zero observed startup errors. No authenticated payment performed; P05 remains unresolved. Evidence: ignored `.codex-local-evidence/scanner-polish-device-update.json`, `scanner-polish-source-hashes.json`, `scanner-polish-tests.log`, `scanner-polish-typecheck.log`, `scanner-polish-lint.log`, `scanner-polish-android-build.log`.

## Scanner-first Android update — 22 September 2026, 15:26

Installed at **15:26:53 MESZ** on Android ending **8690**, package `com.opago.wallet.productioncandidate`. Installed SHA-256 **`e641a06a1096904d45f9c4a5b5c24dfb5730bba31360f6174a57964c769d3345`** matches the verified APK. APK v2 signature, 70 bundled source modules and native seed module checked; preview fixtures excluded. Original first-install timestamp remains 16 September 2026, 15:42:19. Locked cold launch **2969 ms**, zero observed process startup errors; not a balance-load measurement or authenticated payment test.

Send opens the camera directly with manual entry and paste as alternatives; Home uses the same scanner. Automatic detection covers Lightning, Bitcoin onchain and supported HBAR requests, and a different detected asset discards the prior asset's amount. 311 app tests, TypeScript and changed-file lint passed; the final scanner/send subset passed 15 tests after accessibility labels were added. Synthetic browser presentation checked at 360×740 and 320×568. Live-camera, clipboard and back-navigation acceptance of this new version still require the owner to unlock the device. No real payment sent. Evidence: ignored `.codex-local-evidence/scanner-first-device-update.json`, `scanner-first-source-hashes.json`, `scanner-first-tests.log`, `scanner-first-final-targeted.log`, `scanner-first-android-build.log`.

## Pending-send recovery follow-up — 22 September 2026

A real 20-SAT send (maximum 2-SAT fee) was interrupted by network loss and reached the pending warning. Opago was then force-stopped while offline and restarted after connectivity was restored. The owner confirmed no new recipient payment; Home/history continued to show a pending record. This is not a passed P05 result.

The follow-up adds a shared, paginated provider-request lookup independent of transfer history, preserves discovered request IDs through history failures, and clarifies pending history titles, timestamps and Home notices. 287 app tests, TypeScript and changed-file lint passed. APK v2 signature and 67 bundled source modules verified; update installed at 12:05:02 on Android ending 8690 with the original first-install timestamp unchanged. Installed SHA-256: `f0e6d26904362ea678f0f56a6e046f7098c7c320b1a6f6eb2c7eec30c8997e0d`. Locked cold launch: 3147 ms, zero observed runtime errors; this is not an authenticated balance measurement. Internal opt-in diagnostics emit only fixed recovery outcomes. The owner rechecked the existing payment after unlocking and waiting at least one minute. The 11:45 record remained pending; filtered diagnostics showed repeated successful lookups with no matching provider request or transfer. This is an unresolved integration boundary, not evidence of failure. No replacement payment was submitted. Evidence: `.codex-local-evidence/p05-recovery-device-update.json`, `p05-recovery-source-hashes.json`, `p05-recovery-tests.log`; [device acceptance](TEST_ACCEPTANCE_2026-09-22.md).

The next correction adds the SDK 0.7.12 read-only `queryHTLC` lookup with explicit sender role and exact payment-hash binding. Only a verified preimage can confirm a payment through this fallback. Empty, returned, mismatched or unproven operator records never release the resend guard. Home/history describe the result as unknown. The lookup runs alongside provider-request retrieval, after the primary Bitcoin balance. 292 app tests, TypeScript and changed-file lint passed; device acceptance remains open. Android update installed at 12:51:07 MESZ, original first-install timestamp unchanged. APK SHA-256 `712c9b5371aa05f04e3868eb414029c90374028f083bfa7a78472a478babdbe5` matches the installed artifact; v2 signature and 67 source-map modules verified. Locked cold launch 2893 ms, no observed startup errors (not a balance measurement). Evidence: `.codex-local-evidence/p05-operator-device-update.json`, `p05-operator-source-hashes.json`, `p05-operator-tests.log`. The owner rechecked the existing record: still unknown. Each of the three read-only data sources returned no matching record in 26 captured passes. The fixed-stage recorder was stopped afterwards. This establishes missing remote matches, not a terminal unpaid result. No invoice, amount, identifiers or raw logs were recorded.

The next correction moves final read-only balance/fee/expiry/authorization checks ahead of the durable pending write, avoiding ambiguous records caused solely by a preparation-time process death. The journal still blocks duplicates before SDK submission; authorization and expiry are rechecked after persistence. Failed preflight does not modify a prior unknown attempt. 296 app tests, TypeScript and changed-file lint passed. The older real attempt is not cleared or reclassified. [Provider integration findings and prepared questions](SPARK_PENDING_SEND_INTEGRATION.md) document the remaining blocker; no external message was sent. Latest Android update installed at 13:05:57 MESZ without resetting app data. Installed APK SHA-256 `f8b666eaada7538ddb35fdc8821b7d684a9236c17d41714306b7bbabb2981456` verified against the built artifact; v2 signature and 67 bundled source modules match. Original first-install timestamp unchanged; locked cold launch 2864 ms and no observed startup errors (not a balance measurement). Evidence: `.codex-local-evidence/p05-preflight-device-update.json`, `p05-preflight-source-hashes.json`, `p05-preflight-tests.log`. No new real payment or another unlock-and-wait test was requested.

Owner-requested presentation removal: an unresolved Lightning entry can now be hidden from normal history and its persistent notice dismissed. Only optional visibility metadata changes; the original pending record, duplicate guard and read-only reconciliation remain intact. Hidden entries remain accessible and terminal outcomes reappear. Concurrent visibility edits do not discard an authoritative resolution. 301 app tests, TypeScript and changed-file lint passed; Android update installed at 13:26:05 MESZ, SHA-256 `0f31d6dd33a0126db89745f6788a5e070faa8c43cf0b95a913b00e3b133c9379` verified against the installed artifact; v2 signature and 67 bundled source modules checked. First-install timestamp unchanged; locked cold launch 2891 ms, no observed startup errors. Hiding the requested existing entry awaits the owner unlocking the app. Evidence `.codex-local-evidence/p05-hide-device-update.json`. This is not P05 settlement acceptance.

## Home presentation follow-up — 21 September 2026, 13:59

Bitcoin icon/empty-badge fix installed at **14:31:23** on Android ending **8690**. Empty network labels no longer render a pill; the shared Bitcoin icon uses the Bitboy SVG without a pale border. TypeScript, lint, all 270 tests and the Android build passed. Vector artwork inspected at 24/44/64 sizes; 65 bundled source modules and APK v2 signature verified. Installed SHA-256: `7945015707b0415ee45ce382f4fdb07690f376dcb19a7cf9f60cdca63a9dc128`; update preserves the original first-install timestamp. App launch returned success with zero observed process errors, but Android delivered the intent to an already running activity, so this is not a cold-start timing measurement. No authenticated wallet screenshot or payment performed. Ignored evidence: `.codex-local-evidence/asset-icon-device-update.json`, `asset-icon-source-hashes.json`, `asset-icon-tests.log`, `asset-icon-android-build.log`, `asset-icon-preview.png`.

Subsequent BTC price display update installed at **14:05:50** on the same device. The card now shows the existing feed's BTC/EUR unit price, with loading/unavailable/stale labels, instead of the route-description subtitle. TypeScript, lint and all 270 tests pass; 65 bundled source modules, APK v2 signature and installed hash verified. SHA-256: `ad136112f48d33480b9b6d2115202d6207b7839b9230e47ea11e4d2a714535e8`. Wallet data preserved; locked launch 2941 ms, no observed runtime errors. Ignored evidence: `.codex-local-evidence/home-price-device-update.json`, `home-price-source-hashes.json`, `home-price-tests.log`, `home-price-android-build.log`.

At the owner's request, Home restores the previous compact layout and three circular Send / Swap / Receive actions, while retaining the balance, reconciliation and security corrections. Swap still shows its existing coming-soon notice. TypeScript, lint and all 270 tests passed. Android update installed at 13:59:45 on device ending 8690; first-install timestamp and wallet data preserved. APK v2 signature and 65 bundled source modules verified, preview fixtures excluded. Installed SHA-256: `52292175a137533d3f30982807fb29e3711e46c9241c5c5b7cbdc5351a407f19`. Locked cold launch: 2757 ms, zero observed runtime errors. No authenticated payment performed. Ignored evidence: `.codex-local-evidence/home-restore-device-update.json`, `home-restore-source-hashes.json`, `home-restore-tests.log`, `home-restore-android-build.log`. Earlier UI screenshots show the superseded Home layout.

## Bitcoin UI and onchain adapters — 21 September 2026

The new native interface uses one Bitcoin asset and the validated available Spark balance. Real withdrawal/deposit-claim SDK adapters, fees, device authorization and persistent reconciliation are implemented. 270 app tests, TypeScript, lint and the Android build passed. Native view evidence uses an isolated Android package and synthetic data; it is not a real payment acceptance result.

Final artifact installed at **13:00:05** on Android ending **8690** as an update to `com.opago.wallet.productioncandidate`. First install remains 16 September 2026, 15:42:19. APK SHA-256: `1c6bb0dac6011ef83291d366b98f6d7e120e4c6eedde555fbca60465d3655a02`; installed hash matches. APK v2 signature verifies, 65 bundled source modules match the working tree, preview fixtures are excluded and the native seed module is present. Cold locked launch: 2818 ms, zero observed runtime errors. This is not an authenticated balance/performance measurement. Evidence: ignored `.codex-local-evidence/bitcoin-ui-device-update.json`, `bitcoin-ui-source-hashes.json`, `bitcoin-ui-android-build.log` and `bitcoin-ui-tests.log`.

**Public release remains blocked.** The common receive URI is intentionally not emitted: the app provides a Lightning request plus a separate Bitcoin address until unique request mapping, fee semantics and sender compatibility are proven. Mainnet withdrawal/claim acceptance, definitive provider-failure handling, recovery without a local onchain journal and iOS/accessibility tests remain outstanding. See [BITCOIN_PAYMENT_ACCEPTANCE.md](BITCOIN_PAYMENT_ACCEPTANCE.md) for SDK 0.7.12 findings, migration details, test coverage and named external-wallet cases. The earlier P01–P05 evidence below is retained as historical evidence.

## P01–P05 internal Android update — 21 September 2026, 11:52

The final candidate built successfully and passed APK v2 signature verification. Forty-eight source-map modules match the working tree, including the payment changes and unchanged startup/key-derivation modules; local preview fixtures are excluded. Installed as an update on Android ending 8690 at 11:52:35, preserving the first-install timestamp and wallet data. Installed APK SHA-256 matches `8d26462d27fd7ef790db5d9273d3e12b3d6089d7ebc25ef5ebab66aa8428e18f`. Cold locked launch succeeded in 2780 ms with zero observed app-process startup errors. This is not an authenticated balance or payment measurement.

Ignored local evidence: `.codex-local-evidence/p01-p05-device-update.json`, `p01-p05-source-hashes.json`, `p01-p05-android-build.log`, `p01-p05-tests.log`. The build includes the final pre-submission fee-change handling. No payment or wallet recovery was performed; physical payment acceptance remains open.

## Review findings and disposition

| Finding | Implemented in this working tree | Evidence still required |
| --- | --- | --- |
| Payment authorization and app lock | Strong biometrics or Android 11+ system credential approval for both assets; background/idle lock; session-bound final submission guard; SDK connection disposal and stale-startup protection | Physical authentication cancellation, device PIN unlock, idle, background, process death and enrollment-change matrix |
| Backup onboarding | Mandatory choice to back up or explicitly accept postponement; wallet-bound three-word verification; persistent reminder; no automatic replacement of inaccessible keys | Clean-device recovery of both Mainnet assets and backup/restore accessibility |
| False HBAR failure after success | Haptics isolated from payment outcome; duplicate button submission guarded | Confirmed-payment display on physical devices with unavailable haptics |
| Mainnet acceptance | Existing journals/reconciliation retained; first HBAR deposit explicitly describes key-alias compatibility | All Lightning scenarios; second-device Mainnet recovery; named/versioned compatible external HBAR senders |
| Independent audit | Source, tests and artifact evidence prepared; no audit claimed | Mobile/key-lifecycle/native-build and deployed checkout-contract reviews, remediation and reviewer sign-off |
| eID and merchant identity | Identity-payment paths reject before identity data is sent; external links restricted to checkout review; merchant not presented as verified | eID is outside this release; any future activation requires a separate partner/security acceptance. Generic checkout must retain its unverified-identity explanation |
| Product promises | Swap action shows a coming-soon notice only; onboarding says “Hold. Pay.”; Bitcoin/Lightning by default, HBAR under Advanced options | Store description/screenshots must match the final artifact |
| Release and operations | Dependency remediation, reproducible compiler inputs, release/runbook updates, exact internal artifact evidence | Clean reviewed commit, isolated install/build, store signing, signed update test, named support/incident owners, monitoring and rollout exercise |

## Automated evidence

P01–P05 follow-up, 21 September 2026: 245 app tests, TypeScript and changed-file ESLint passed. New coverage exercises exact invoice network/signature/default-expiry checks; LNURL amount and metadata commitments; fresh pre-submit balance and unchanged fees; repeated confirmation, unknown outcomes and timed-out SDK calls; stale reconciliation races; receive recovery after expiry, storage failures and success dismissal; pending-send recovery after Bitcoin loads; and scans before SDK readiness/after leaving Send. These use synthetic data and do not establish real-funds acceptance. The updated [Lightning acceptance runbook](LIGHTNING_MAINNET_ACCEPTANCE.md) separates the remaining physical checks.

- `npm test`: 245 passed, zero failed (21 September 2026); two native seed-derivation tests also passed.
- `npm run contract:test`: nine passed with Hardhat 3; no chain transaction is submitted by the test runner.
- TypeScript and ESLint pass; `npx expo install --check` reports compatible SDK 54 package versions.
- Full and production-only npm audits: zero known advisories. Ignored local reports: `.codex-local-evidence/release-security-audit-all.json` and `release-security-audit-prod.json`.
- Solidity `0.8.28`, optimizer 200, Paris, original CRLF sources: runtime SHA-256 `18dfd309cde03d2291101f3b77f8c5810664a5c52bbed3b63ccce4752d7943c8`. Matches verified Mainnet contract `0.0.10850063`; no contract redeployment.
- Regression cases include cancellation before submission, locking during journal persistence, stale SDK initialization/disposal, idle expiry, PIN authorization, biometric-only rejection, PIN cancellation and foreground/session binding, haptic failure, wallet-bound backup state, malformed native intents, router query compatibility, and identity-path rejection without a network request.

Local logs are development evidence, not a clean-commit or app-store acceptance. The official `android:production-candidate` script still requires a clean Git worktree. Internal device updates built from this working tree must be identified as such.

## Internal Android update — 17 September 2026

The final arm64 release build succeeded and its APK v2 signature verified. Package `com.opago.wallet.productioncandidate` was updated on the connected UMIDIGI Android device at 12:45 local device time using `install -r`; no wallet removal, uninstall or data clear was performed. Cold launch succeeded, zero app-process runtime errors were observed, and the new locked-wallet screen was visually confirmed. Eleven security-related source modules in the native source map exactly match the working tree.

APK SHA-256: `9b6236f6430891ded41b676df87652077d0707c58f49c361af461c6eb86ca950`. Exact source hashes and installation evidence are in ignored `.codex-local-evidence/release-security-device-update.json` and `release-security-source-hashes.json`. Native unlock and Security interaction await the owner's device authentication. This update does not establish recovery, accessibility or public release acceptance.

## Follow-up internal Android update — 17 September 2026, 13:53

The combined balance/scanner/language APK built successfully, passed v2 signature verification and updated the same candidate package with `install -r` at 13:53:51 local device time. The original installation timestamp remains unchanged. Cold launch succeeded in 3471 ms, with zero app-process runtime errors observed after startup and camera use. Fourteen changed source modules and all three 350-message catalogs in the native source map match this working tree; no local preview fixtures are bundled.

APK SHA-256: `dcacf5f876d1c7c3c9171dc30daf8f5b3b3a31f9c5fdb589ee41a8e6be71cb40`. Evidence: `.codex-local-evidence/language-device-update.json`, `language-source-hashes.json` and `language-android-build.log`. The native German Home loading state was visually confirmed. Android's camera service confirmed an active camera client for Opago; camera closure and a still-accessible German Request screen were observed afterward. No camera-feed image was captured. Repeated opening and actual QR decoding still need the owner's acceptance; native language selection/persistence was not manually exercised.

## Balance, scanner and language acceptance

Home and Send now distinguish unknown/loading balances, confirmed zero, last-known values and refresh failures. The EUR total is withheld while either asset balance is unknown. Separate balance requests publish independently of slower history requests and ignore superseded responses. The Home QR action opens a dedicated camera route directly; granted camera access is read without calling the Android permission request again. Scan results stay in a bounded, one-use, session-bound in-memory inbox, rather than in navigation URLs. Background and idle locking remain enforced.

Security → Language offers English, French, Spanish and German. The choice is stored separately from wallet secrets, is loaded before the wallet UI, and applies without resetting the wallet session. Unsupported system languages fall back to English. Each of the three translated catalogs contains 350 messages; recovery words, addresses, identifiers and signing data remain unchanged. Unknown third-party diagnostic text may retain its original language.

Automated checks cover unknown/zero/stale amounts, invalid balance responses, repeated scanner permission checks, scan expiry/lock invalidation, language persistence and failed saves, regional defaults, placeholder parity and preservation of recovery/payment values. Isolated browser checks with synthetic data confirmed the four language choices, persistence after reload, loading/offline layouts, French Send and Spanish recovery validation at 320/360 px. The selected language is exposed as a radio state. Native QR decoding, repeated camera opening, TalkBack and owner-wallet language selection still require physical acceptance; no automated payment or recovery was performed.

## Security screen acceptance

The page now leads with the persisted backup status and guided save/check actions. Removal authorization is separate and expires with the app session. Words use scalable native text, numbered accessible items and one/two responsive columns; recovery remains protected from capture and auto-hides. App protection explains background/idle locking; networks and public keys are at the bottom under Advanced wallet details.

The isolated browser fixture was visually checked at 360 and 320 pixels using public synthetic words only. It passed the three-word flow and displayed the correct success status without deletion copy. Evidence: `.codex-local-evidence/security-backup-first.png` and `security-recovery-synthetic.png`. This is not a native VoiceOver/TalkBack acceptance; large system text and screen-reader behavior still require the device matrix below.

## Two-device recovery acceptance

Use the existing first device as the reference; do not remove its wallet. The second device must have no wallet in this candidate package. If it already has one, stop and resolve its backup before any replacement. Installing an update uses `adb -s <serial> install -r` and must never clear package data.

1. Record both device models, Android versions, artifact/package identifier and test time. Keep device serials in private local evidence only.
2. On the first device, complete the paper-backup check. The user reads and writes recovery words privately; neither automation nor support captures them.
3. Install the same accepted candidate on the second device. Set up supported biometrics or an Android 11+ device credential, choose “I already have a wallet”, choose the correct phrase length, and enter each numbered word. Do not paste words into chat, terminal, screenshots or shared documents.
4. Compare the same HBAR numeric account and balance after Mirror Node synchronization, and the same Bitcoin/Lightning balance and history after Spark synchronization. A loading failure must remain an error, not be treated as a zero balance. Record only redacted pass/fail evidence.
5. Lock/reopen, force-stop/reopen and reboot the second device. Confirm that authentication is required and both accounts remain the same. Check backup reminders and permissions after restoration.
6. Complete the send/receive and failure scenarios in `LIGHTNING_MAINNET_ACCEPTANCE.md` using human-approved small amounts. A matching balance alone does not prove that signing or ambiguous-payment recovery works.
7. Repeat the HBAR receive/send/restart checks and record the result. Do not erase the reference wallet as part of this test.

Current status: second device available according to the user; installation and recovery results must be recorded only after observation. No successful second-device Mainnet recovery is claimed here.

## Physical security and usability matrix

For every supported Android version/model, record pass/fail for cold unlock, PIN fallback, strong-biometric success/cancel/failure, missing/weak biometrics, two-minute idle lock, Home/app switching during review/auth/submission, process death, and update preserving keys/account/journals. Use a disposable test wallet for enrollment-change, lost-key and uninstall cases; never experiment on a funded wallet without a separately checked backup.

Test onboarding, backup, restoration, lock, full recipient review and success/reset screens with small displays, largest supported text, keyboard open and TalkBack. Confirm labels, focus order, errors, scroll reachability and that amounts/recipients never truncate misleadingly. iOS distribution requires its own complete native acceptance; Android evidence does not approve iOS.

## Public artifact and operational sign-off

Before distribution, the release owner records one reviewed commit, lockfile hash, isolated `npm ci` result, SDK/native build versions, production network configuration, APK/AAB SHA-256, store signing certificate and reviewer. Run `npm run phase5:verify`, fresh dependency/secret review and the acceptance matrix on that exact artifact. Preserve pending journals through the signed update test; never clear application storage to make an update pass.

Opago must name the release approver, security/incident lead, support responder, service-health responder and rollout-stop approver, with actual contact channels and coverage. No assignment or staffed service is assumed by this document. Exercise a simulated Spark outage, Hedera Mirror outage and unknown payment using `LIGHTNING_OPERATIONS_RUNBOOK.md`: classify the incident, stop rollout, issue an approved user notice, reconcile outcomes, and resume only with recorded approval. The app currently has local diagnostics; centralized monitoring and remote rollout controls have not been provisioned.

Independent reviewers need the exact source/native artifact, dependency lock, derivation/storage/authentication design, session lifecycle and journals, malformed QR/deep-link cases, contract source/compiler input/deployed runtime, and the completed device evidence. Track findings to closure before declaring public readiness. Zero npm advisories and internal functional tests do not close this gate.

## LNURL regression — 17 September 2026

A supplied Wallet of Satoshi reusable LNURL exposed a preparation error: its variable amount range was treated as an invalid request when no amount had been entered after scanning. `resolveLightningDestination` now returns an explicit amount-input step. The Send form focuses that field, displays recipient limits and requires an amount before proceeding. Fixed invoices and fixed LNURL ranges retain immediate preparation; the callback's invoice must match the selected amount, as required by [LUD-06](https://github.com/lnurl/luds/blob/luds/06.md). Fee policy and biometric submission are unchanged.

Specific localized errors distinguish invalid amounts, mismatching requests, unavailable fee estimates and fees over the existing cap. The exact user's LNURL was tested live only for endpoint resolution and requesting an unpaid 20 SAT invoice; no keys or payment submission were used. Evidence `.codex-local-evidence/lnurl-live-resolution.json` omits the recipient identifier and invoice. Six new regression tests cover amount selection, callbacks, request variants, invalid limits, amount mismatch and fee failure. All 150 application tests pass, as do TypeScript and ESLint. Browser evidence `.codex-local-evidence/lnurl-amount-de.png` uses synthetic data. A subsequent device screenshot identified the second failure as a 2 SAT quote exceeding the former 1 SAT percentage budget; see the fee correction below. No successful device payment has been verified.

The corrected arm64 APK was installed on the same Android device with `install -r` at 14:13:58, retaining the original first-install timestamp. APK v2 signature verification passed; SHA-256 `e1f6e3bd05aba149361531af861c9120a384dc04208fdc5377997a90bf8aa097`. Eleven changed modules/catalogs in the native source map match the current sources. Cold launch completed in 3110 ms, with zero observed JavaScript/fatal startup errors. The German locked-wallet screen was confirmed; device authentication and a real payment remain with the owner. Evidence: `.codex-local-evidence/lnurl-device-update.json`, `lnurl-source-hashes.json`, `lnurl-android-build.log` and `lnurl-android-start.png`.

## Lightning fee correction — 17 September 2026

The second user screenshot confirms that quote retrieval succeeds but the former 0.5% fee budget rejects a 2 SAT quote for a small payment. A valid quoted fee is now used as the exact review/submission maximum, bounded by the existing configured 100 SAT ceiling and available balance. Review explicitly displays maximum network fee and maximum total before the existing send action and biometric authorization. Submission receives the same reviewed maximum, never an automatically increased allowance. With no fee-estimate method, the conservative percentage fallback is retained; invalid/unavailable estimates remain blocking.

All 155 tests pass, including exact reviewed-fee submission, small payments, exact available balance, zero fees, amountless invoices, invalid estimates, build ceiling, insufficient balance and authorization cancellation. TypeScript and ESLint pass. The German review was visually checked at 320 px with synthetic values (20 SAT + maximum 2 SAT = maximum 22 SAT). Evidence: `.codex-local-evidence/lightning-fee-tests.log`, `lightning-fee-typecheck.log`, `lightning-fee-lint.log`, `lightning-fee-review-de.png`. Real fee changes during submission and a completed owner-authorized payment remain physical acceptance items.

The fee-fix arm64 APK built successfully, passed v2 signature verification and matched seven changed source files/catalogs in the native source map. SHA-256 `357baa5db7d1ae01a3dd13cf9a36822749f589315d657758d32648bca0bf3d31`. It updated device `0310841080358690` using `install -r` at 14:23:05, preserving the first-install timestamp; cold launch succeeded in 3132 ms with zero observed JavaScript/fatal startup errors. The second device `0310841080358685`, which already had the candidate package, disconnected during installation; its update did not complete. Evidence: `.codex-local-evidence/lightning-fee-device-update.json`, `lightning-fee-source-hashes.json` and `lightning-fee-android-build.log`. No real payment was submitted.

## Android device-PIN support — 17 September 2026

The owner explicitly approved system-device-PIN support after a second Android device could not create a wallet with the former biometric-only policy. Both attached devices report Android API 34. Android 11+ now uses the strong-biometric-or-device-credential system prompt for creation, restoration, payment approval and recovery disclosure/backup checks. Weak face recognition is not accepted by itself, and Opago never receives the PIN. Older Android PIN-only devices get a compatibility message; strong biometrics remain supported. Existing stored keys are not migrated or weakened; see SECURITY.md for the storage boundary.

The session exception is limited to an outstanding native authentication attempt; it revokes older approvals, blocks foreground-sensitive work while backgrounded, requires native success and an active app, and cannot survive cancellation, a lock/re-unlock or the two-minute idle expiry. Ordinary background locking remains. A separate guard keeps already-authorized SDK initialization valid across the temporary prompt without allowing it to sign or disclose keys.

All 165 application tests pass, as do TypeScript and ESLint. Added cases cover PIN-only and weak-biometric enrollment, native cancellation, pre-prompt backgrounding, delayed foreground restoration, foreground timeout, old Android compatibility, lock/re-unlock, expiry, one-use prompt completion and preserving SDK startup while invalidating action approvals. No device credential was entered by the agent and no real payment was submitted. Real PIN creation/restore/payment and the native lifecycle matrix still require owner acceptance.

The PIN-support arm64 APK passed v2 signature verification and matched all eight changed modules/catalogs in its native source map. SHA-256 `45f211b35c7c23619bbc453efd53d74bb2102dbb85d15e5b625780506a615d51`. Both attached Android 14 devices were updated with `install -r` (14:45:54 and 14:47:25), retaining their original first-install timestamps and without clearing package data. Both app processes launched, with zero observed JavaScript/fatal startup errors. The second-device screenshot was black; native PIN-dialog appearance and successful credential entry were not verified. Evidence: `.codex-local-evidence/device-pin-device-update.json`, `device-pin-source-hashes.json` and `device-pin-android-build.log`. Creation, restoration and payments still require the owner to enter the device credential privately.

## Backup loading, Swap placeholder and actual inactivity — 17 September 2026

Backup state starts as loading and resets to loading when the wallet runtime clears. Only the persisted record bound to the current wallet decides verified/deferred/required. Home does not show a missing-backup warning during hydration; Security and Request use a neutral loading/retry notice. A premature backup action cannot overwrite loading or verified state.

The owner requested restoring Send / Swap / Request. Swap now shows a localized coming-soon alert without invoking any swap or payment integration. Activity handling observes foreground gesture capture and movement, software/hardware keyboard events, modal controls and accessibility/button/tab activation. The existing session expires 120 seconds after the last accepted user event; rendering and network work cannot keep it alive. Expired sessions remain locked, and ordinary background locking and payment authorization remain enforced.

All 170 application tests, TypeScript and ESLint pass. Five new regressions cover sustained mixed interaction over six minutes followed by exact inactivity expiry, callback/ref/gesture preservation, ignored non-user/background events, delayed persisted backup hydration and reminder rendering. Evidence: `.codex-local-evidence/home-idle-tests.log`, `home-idle-typecheck.log`, `home-idle-lint.log`. Native keyboard/gesture and two-minute owner acceptance remains separate from these automated checks.

The arm64 APK built successfully, passed v2 signature verification and matched 15 changed modules/catalogs in the native source map. SHA-256 `a47a20fbe2da5122aa3d86bf144f7ff0ea9911b7c596280d2fd6620cdadccd30`. Both Android 14 devices updated with `install -r` at 15:08:33 and 15:08:54, retaining original first-install timestamps. Both cold launches completed (3877 ms and 3041 ms), with zero observed JavaScript/fatal startup errors. Evidence: `.codex-local-evidence/home-idle-device-update.json`, `home-idle-source-hashes.json`, `home-idle-android-build.log`. User credentials, wallet recovery and real payments were not exercised.

## HashPack first-deposit incompatibility — 17 September 2026

A newly generated Opago wallet has an Ed25519 public key before any Hedera account exists. The SDK key alias `0.0.<DER public key>` is valid for an SDK alias transfer, but it is not a numeric account ID. HashPack web v14.4.1 rejects that 92-character value with `Unknown account format`; it also rejects the same Ed25519 public key in raw (64 characters) and DER (88 characters) form. Removing `0.0.` does not solve the compatibility issue. Its public recipient validator accepts an ECDSA DER key and numeric account IDs. This was reproduced offline against the exact public `checkAccountExists` method with dialogs/network stubbed, not by submitting a payment. Local evidence: `.codex-local-evidence/hashpack-format-reproduction.json`. A read-only Mainnet public-key lookup found no account for the reported wallet at investigation time; the user’s key is omitted from versioned evidence.

The receive-screen source now shows a clear activation requirement and copyable public-key details instead of presenting the incompatible alias as a payment QR. Existing activated accounts retain their numeric QR. This UI correction is not an activation service and has not yet been installed on the devices. The product decision and funding for one-time native Hedera account creation remain pending. No private key, recovery phrase, funded account creation or payment was used during this investigation.

Ordinary Opago HBAR payments already use `TransferTransaction` directly. The deployed contract is an optional, separate merchant-checkout path, unrelated to first-account activation. Native `AccountCreateTransaction` or a supported alias transfer can establish the first numeric account; neither requires a smart contract. Preserve the existing recovery derivation when adding account activation rather than silently changing existing wallets from Ed25519 to ECDSA.

The owner raised cost concerns and has not approved an Opago-funded activation service. The current Hedera fee table lists a standard CryptoCreate base fee of USD 0.05 (https://docs.hedera.com/networks/fees), with transaction-specific extras possible. Hedera alias auto-creation can instead charge the transfer payer (https://hedera.com/blog/how-to-auto-create-hedera-accounts-with-hbar-and-token-transfers/). A sender-funded first deposit using HashPack-supported ECDSA addressing is a design option for future wallets, not implemented or accepted for the existing Ed25519 derivation. Existing wallets must not silently change keys/account identity. No funding or activation transaction has been executed. The prepared receive UI passes TypeScript, ESLint and all 170 application tests; it remains uninstalled while the activation path is being decided.

## HBAR transfer limit and encoding correction — 17 September 2026

The reported 2 HBAR transfer to a numeric HashPack account was rejected locally because the internal production build still enforced a 1 HBAR test cap. The generic error mapper hid that reason. The owner explicitly chose available balance minus fees. The production Gradle and EAS profiles now set `EXPO_PUBLIC_HEDERA_MAX_TRANSFER_HBAR=balance`; numeric caps remain available for limited test builds, and a missing/invalid Mainnet policy still fails configuration.

Both review and the refreshed account check before signing enforce amount plus maximum network fee. The direct-transfer ceiling remains 0.1 HBAR and the merchant-checkout ceiling 0.75 HBAR. Amounts retain exact bigint precision and must fit the SDK's positive signed-int64 range. No payment authorization, final session guard, pending journal, checkout integrity check or recovery derivation was removed.

Broken UTF-8 punctuation was corrected in Send, Security and recovery entry, including strings whose damaged English lookup keys prevented translation. Limit/amount errors now have specific English/German/French/Spanish messages. All 174 application tests, TypeScript and ESLint pass. A fresh-process Mainnet regression uses a stubbed SDK transport: 2 HBAR fits 2.38690992 HBAR, the exact balance-minus-fee amount passes, one extra tinybar fails, checkout uses its own fee reserve, and cancellation prevents submission. An explicit scan detects broken punctuation in bundled sources/catalogs. Evidence: `.codex-local-evidence/hbar-send-tests.log` and `hbar-policy-profile-tests.log`. No real transfer or device credential was used.

The working-tree arm64 APK built successfully and passed v2 signature verification. Its 13 changed modules/catalogs match the native source map; local preview fixtures are absent and the APK contains the verified compiled bundle. SHA-256 `0b951f36b2c4e6450f8add150997ebc9a1cb62121a5bdd49cbfb06db031e84b8`. The streamed `install -r` attempt on device `0310841080358690` failed after ADB reported a closed connection. Reconnecting reported unauthorized and the device then disappeared. Installation and cold launch are not confirmed; the owner was asked to reconnect/unlock and approve USB debugging. No package data was cleared. Evidence: `.codex-local-evidence/hbar-send-device-update.json`, `hbar-send-source-hashes.json`, `hbar-send-android-build.log`.

This artifact also contains the previously prepared receive-screen activation explanation. It does not implement ECDSA wallet derivation, change existing keys, or perform paid account activation; the first-deposit design remains pending.

After the owner reconnected and approved debugging, ADB still alternated between device/unauthorized and closed every shell request. Restarting the local ADB server did not resolve it. The ADB log confirms repeated USB read failures and write I/O errors; the owner was asked to try another cable or direct PC port. The APK remains ready for installation.

### Installation confirmed — 17 September 2026, 15:44:55

After the user reconnected the device, a package check confirmed it still had the 15:08:33 build with the old transfer cap. The verified HBAR correction APK then installed successfully on `0310841080358690` using `install -r --no-streaming`. Its on-device `base.apk` SHA-256 exactly matches `0b951f36b2c4e6450f8add150997ebc9a1cb62121a5bdd49cbfb06db031e84b8`. The first-install timestamp remains 16 September 2026, 15:42:19; no app data was cleared. Cold launch completed in 3203 ms and zero JavaScript/fatal startup errors were observed. The earlier USB installation block is resolved for this connected device. The second device was not connected and has not received this update. No real payment or device credential was entered; owner-authorized transfer acceptance remains pending. Evidence: `.codex-local-evidence/hbar-send-device-update.json`.

The same verified HBAR update also installed on the second device `0310841080358685` at 15:51:07 using `install -r --no-streaming`. Its on-device APK hash matches the artifact above; first install remains 17 September 2026, 14:16:13. Cold launch completed in 2986 ms with zero observed JavaScript/fatal startup errors. No data clear, credential entry or real payment. Evidence: `.codex-local-evidence/hbar-send-second-device-update.json`.

## Balance startup follow-up — 17 September 2026

The owner confirmed that Home appears but balances take too long after unlocking. The connected original device was verified to already have the 16:06:36 home/navigation build, so this is not a stale installation. Code inspection found that Spark initialization synchronizes balances and our first display then immediately repeated the network query. Home also lost all last-known balances when the lock screen unmounted it.

Home now reads an encrypted, wallet/network-bound balance-and-rate preview after key identification and explicitly labels it as last known while live updates run. The preview contains no signing keys, expires after seven days using the original observation timestamps, and is removed with the wallet. Serialized storage writes and session guards prevent queued writes from surviving removal or a lock. Preview values are used only in Home; payment review and signing retain independent fresh reads. Spark's completed startup synchronization is consumed once for the first display (only within five seconds of initialization), with a fresh-read fallback and subsequent live refreshes. Transient Mirror Node failures retain the account binding instead of starting duplicate discovery.

The owner additionally requested collapsed, on-demand transaction history. Home now performs no local history, journal or remote history reads until Recent activity is expanded, and a pull-to-refresh with activity collapsed refreshes balances only. Lightning pending reconciliation has moved from SDK startup to opening Send or expanding history; pending-payment duplicate protection is retained. 188 application tests, TypeScript and changed-file ESLint pass, including a test that renders the actual Home component, opens/closes the section and checks which data sources run on refresh.

Local diagnostic logging is opt-in for this internal build and emits only fixed stage labels and elapsed milliseconds after successful device authentication. No keys, addresses, balances or request contents are logged by this instrumentation. The first measured unlock on the preceding balance-preview build reached keys at 7783 ms, HBAR at 14700 ms, Lightning at 18633 ms and the complete EUR display at 18873 ms. This is an initial-cache-fill observation, not a before/after performance guarantee. A cached reopening measurement is pending.

The final build including on-demand history installed on `0310841080358690` at 16:25:58 without clearing app data. Its on-device SHA-256 matches `2db96ca52e4c08a276f94cf28e7256d92782eae009b7659af8a5a7f646095749`, v2 signature verification passed, and 26 source/catalog modules match the native source map. First install remains 16 September 2026, 15:42:19. Locked cold launch was 2802 ms and no JavaScript/fatal startup errors were observed. This locked-launch measurement does not measure balance readiness. The second Android device still needs this current artifact. Evidence: `.codex-local-evidence/balance-startup-device-update.json`, `balance-startup-source-hashes.json`, `balance-startup-device-timings.jsonl` and `balance-startup-tests.log`.

## Home responsiveness and navigation — 17 September 2026

Wallet derivation and optional Lightning initialization now yield past a UI frame before synchronous work, with a bounded fallback for paused animation frames and session revalidation before accessing keys. This changes scheduling, not cryptographic derivation or the authentication requirement. Concurrent HBAR display reads share an in-flight request; the pre-signing lookup does not use this optimization. Home activity sources start concurrently and publish each completed batch without waiting for a slower source. Source order is stable, failed sources retain already visible activity, refreshes are bounded to eight seconds, and screen-generation guards ignore obsolete responses.

Send uses a visible Back arrow/label, an asset-specific heading and a full-width recipient field. Both payment reviews have a visible top Back action. Android Back moves from review to details, then to asset selection, while navigation during payment execution is blocked. The tab bar uses outline icons, a shield for Security, subdued inactive labels and a yellow active tab, adapting to safe-area insets and font scale.

All 179 application tests, TypeScript and ESLint pass. New regressions cover fast data preceding a slow network, bounded failures and ignored late responses, UI-frame scheduling, locking before deferred key derivation, and coalesced display lookups without caching later reads. Browser checks used only synthetic data at 320/360 px and exercised HBAR and Bitcoin forms/reviews and both visible Back steps. Native hardware Back, large native text, TalkBack and authenticated startup timings remain owner acceptance. No device credential or real payment was entered. Evidence: `.codex-local-evidence/home-navigation-tests.log` and `home-navigation-android-build.log`.

The home/navigation arm64 APK built in 2m14s, passed v2 signature verification and matched 16 changed/preserved source modules and catalogs in its native source map. No preview modules were bundled. SHA-256 `fbcf848d6fbfae81783ecb37acc40b6f518fd05412e1f462e1ef4c1c0e9075da`. While work continued, the connected device changed back to the original `0310841080358690`. The new APK installed there at 16:06:36 using `install -r --no-streaming`; the on-device APK hash matches, the first-install timestamp is unchanged, and cold launch completed in 3032 ms with zero observed JavaScript/fatal errors. This startup metric covers the locked app launch, not authenticated Home/network readiness. The second device had received the earlier HBAR correction at 15:51:07 and still needs this newer interface build. Evidence: `.codex-local-evidence/home-navigation-device-update.json` and `home-navigation-source-hashes.json`.

## Bitcoin default experience — 21 September 2026

Home’s main EUR figure values Bitcoin only; HBAR has a separate estimate under Advanced options. The disclosure is initially collapsed on Home, Send, Request and Security. Bitcoin history does not trigger Hedera account/history requests, and HBAR history remains separately opt-in. Send/Request return to Bitcoin after backing out of explicit HBAR selection; explicit HBAR QR/deep-link reviews still identify the correct asset. Recovery derivation, stored keys, signing/authentication and fee policies are unchanged. Seven new tests cover valuations, Bitcoin-only cached prices, disclosure mounting/accessibility, source selection/reset, deferred HBAR balance reads and partially available exchange rates; lazy-history tests also cover HBAR isolation. Browser checks at 320/360 px used only synthetic values. No real payment or credential was entered.

The internal arm64 working-tree candidate built successfully in 2m54s and passed APK v2 signature verification. All 31 selected source/catalog modules match the native source map; preview modules are excluded. Installed on Android `0310841080358690` with `install -r --no-streaming` at 21 September 2026, 10:26:45; on-device SHA-256 matches `f2ec42df2f085b25d9c487e09bc40363cda9f6e2ce073a6b3d5b978ae43c4180`. The first-install timestamp is unchanged (16 September, 15:42:19). Locked cold launch completed in 2938 ms with zero observed JavaScript/fatal startup errors. This does not measure authenticated Home readiness or verify a real payment. The second device was not attached. Evidence: `.codex-local-evidence/bitcoin-default-device-update.json`, `bitcoin-default-source-hashes.json`, `bitcoin-default-tests.log`, `bitcoin-default-android-build.log`.

## Shared history correction — 21 September 2026

The latest Home layout puts Advanced options above one shared transaction history. That history loads and displays Bitcoin and HBAR together when expanded, regardless of the asset disclosure state. Startup still performs no history queries; the main EUR total remains Bitcoin-only. This supersedes the separate activity sections described in the earlier Bitcoin-default update. All 197 application tests, TypeScript and changed-file ESLint passed, including actual Home render/order, lazy reads and mixed remote/local transaction results.

The shared-history internal arm64 build completed in 1m18s, passed v2 signature verification, and matched 31 source/catalog modules with preview code excluded. Installed on Android `0310841080358690` at 21 September 2026, 10:34:32 via `install -r --no-streaming`. The installed SHA-256 matches `1b99479b70d756e4f3f5eadbd31c0c84af4bf02a319ae9b9348f051282ac0033`, and the first-install timestamp remains unchanged. Locked cold launch: 2822 ms, zero observed JavaScript/fatal startup errors. No authenticated payment was submitted. Evidence: `.codex-local-evidence/shared-history-device-update.json`, `shared-history-source-hashes.json`, `shared-history-tests.log`, `shared-history-android-build.log`.

## Spark-priority Home loading — 21 September 2026

The Home balance hook separates Bitcoin and optional HBAR effects. Early expanded sections queue their reads until the first Bitcoin attempt settles, with a bounded 20-second escape for a stalled SDK. Both optional loaders yield to the UI and discard obsolete results. HBAR toggles no longer trigger another Bitcoin balance fetch. The shared history and pull-to-refresh respect the same priority. Send keeps its explicitly selected asset usable without introducing a Bitcoin dependency into payment execution. Added timing markers contain no keys, addresses, balances or invoices. All 208 application tests, TypeScript and changed-file ESLint passed. W06 remains yellow: the authenticated measurements below establish the remaining initialization delay, rather than a completed performance fix.

The internal arm64 candidate built in 1m16s, passed APK v2 signature verification and matched 31 source/catalog modules with preview code excluded. Installed on Android `0310841080358690` at 21 September 2026, 10:48:05 using `install -r --no-streaming`. Installed SHA-256 matches `498bc0bfc6515293a90cf13842d2ed8d8b2c16a2c3276e0d93b60f82638d010c`. First-install timestamp remains unchanged; locked cold launch completed in 2755 ms with zero observed JavaScript/fatal startup errors. This is not an authenticated balance-load measurement. Device timing capture is separately requested from the owner, with local evidence under `.codex-local-evidence/spark-priority-*`.

### Authenticated timing result — 21 September 2026

The owner performed the unlocks; the recorder stored only allowlisted stage names and elapsed milliseconds. A second capture had no deadline and was stopped after the owner's “fertig” message. It contains one complete unlock, not two. Its timer begins after device authentication: secure mnemonic read 51 ms, Hedera key derivation 4212 ms, Spark initialization 6786 ms, and visible live Bitcoin balance at 11805 ms. The balance read after Spark synchronization took 1 ms, using the already synchronized SDK display cache; this is not a 1 ms network synchronization. The saved preview was available at 4861 ms, but the Home value-render effect ran only at 9606 ms. These effect timestamps are instrumentation, not video-confirmed frame timing.

Optional HBAR started at 12825 ms and settled 400 ms later; history started at 14839 ms. Both followed the first Bitcoin balance at 11726 ms. The user reports that both optional sections appeared quickly after Bitcoin. History completion was not instrumented, so no duration is claimed. Because the sections opened after Bitcoin in this sample, early-expansion ordering is covered by automated tests rather than demonstrated by this device sample. Earlier measurements of this same build recorded 19113 ms and 11742 ms to the live balance; these few samples do not establish a percentile or a before/after improvement. The next optimization target is the required key derivation before Spark and the SDK initialization, including delayed preview rendering, while preserving key identity and session cleanup. W06 remains yellow. Evidence: `.codex-local-evidence/spark-priority-relaxed-20260921-105141.jsonl` and `spark-priority-device-timings.jsonl`.

## Shared native seed startup — 21 September 2026

Authenticated startup now derives one BIP39 seed and reuses it for the unchanged Hedera path and Spark initialization. Android uses a local Expo module backed by system PBKDF2-HMAC-SHA512 on the module queue, with a provider known-answer check before use. iOS/web retain asynchronous JavaScript derivation. No key migration, account-number change, new secret cache or relaxed session/signing guard is introduced. Temporary app-owned seed buffers are erased on failure, success and lock; late native results are discarded. In-flight SDK input has its own bounded-to-operation lifetime and is erased when initialization settles. Existing serialized SDK cleanup remains in force.

All 218 application tests, TypeScript and changed-file ESLint passed. Two native Java unit tests verify 12/15/18/21/24-word seed vectors and invalid input. Tests also compare Hedera signatures and all five Spark signing-key families against the preceding implementations, verify retry reuse and exercise lock-during-derivation and lock-during-SDK-startup. Native tests: `modules/opago-wallet-crypto/android/build/test-results/testReleaseUnitTest/`; application evidence: `.codex-local-evidence/shared-seed-tests.log`. Target: current live Bitcoin balance under 7.5 seconds after authentication, preferably 5 seconds. A cached preview does not meet this target; the authenticated results below confirm it on the tested Android device.

The arm64 candidate with the native module built in 3m15s, passed APK v2 signature verification and matched 35 source/catalog modules. The Expo registration list and APK DEX both include `OpagoWalletCryptoModule`. Installed on Android ending 8690 at 21 September 2026, 11:08:29 with `install -r --no-streaming`; installed SHA-256 matches `2d568e461b4f554324c8da15af5d30cae2fa9e69f6962eb1f7582eb980ae496d`. First-install timestamp is unchanged; locked cold launch completed in 2769 ms with zero observed JavaScript/fatal startup errors. This is separate from authenticated balance timing. Evidence: `.codex-local-evidence/shared-seed-device-update.json`, `shared-seed-source-hashes.json`, `shared-seed-android-build.log`. The owner-operated timing recorder has no deadline and captures only approved stage names and numeric durations.

### Shared-seed authenticated timing acceptance — 21 September 2026

The owner completed three unlocks and reported noticeably faster loading. The recorder was stopped on request. All three runs reached the live Bitcoin render-effect marker below the preferred 5-second target:

| Run | Current BTC visible | Cached preview visible | Native seed | Hedera keys | Spark initialization |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 3617 ms | 1226 ms | 55 ms | 34 ms | 2806 ms |
| 2 | 4162 ms | 1709 ms | 83 ms | 56 ms | 3180 ms |
| 3 | 3837 ms | 1643 ms | 42 ms | 44 ms | 3016 ms |

Mean live-balance latency: 3872 ms; median: 3837 ms; maximum: 4162 ms. Relative to the preceding 11805 ms sample, the new mean is about 67% lower. Optional HBAR and shared-history reads started after the Bitcoin balance in all three runs. These timestamps begin after device authentication and measure render effects rather than video-confirmed native frames. Three samples on one device are not a percentile/SLA or evidence for other phones, networks or iOS. W06 is complete for the implemented, tested Android flow; broader device acceptance remains under R02. No payment was made. Evidence: `.codex-local-evidence/shared-seed-timings-20260921-110855.jsonl` and `shared-seed-timing-summary.json`.
